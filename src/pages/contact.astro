---
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
---
<BaseLayout
  title="문의 — 간병연구소"
  description="자료 요청, 협업, 제휴 문의는 이메일로 보내주세요."
>
  <Header />
  <main>
    <div class="page-header">
      <div class="container-prose">
        <div class="eyebrow" style="margin-bottom: 16px;">Contact</div>
        <h1 class="page-title">문의</h1>
        <p class="page-lead">
          자료 요청, 기업·기관 협업, 언론 자문, 학술 공동연구 등
          모든 문의는 이메일로 보내주세요.
        </p>
      </div>
    </div>

    <div class="container-prose" style="padding-top: 52px; padding-bottom: 96px;">

      <!-- Email -->
      <div class="email-block">
        <div class="email-label">Email</div>
        <a href="mailto:contact@caregiverlab.co.kr" class="email-address">
          contact@caregiverlab.co.kr
        </a>
      </div>

      <!-- 문의 유형 안내 -->
      <div class="type-list">
        {[
          { icon: "文", label: "자료 요청",  desc: "특정 주제 브리프, 1-Page Brief, 데이터 요청" },
          { icon: "協", label: "B2B 협업",   desc: "기업·기관 컨설팅, 간병 제도 교육 자료" },
          { icon: "學", label: "산학협력",   desc: "공동연구, 논문 데이터 지원" },
          { icon: "政", label: "B2G 지원",   desc: "정책 자료, 의회·행정기관 브리핑 준비" },
          { icon: "報", label: "언론 자문",  desc: "팩트체크, 전문가 코멘트" },
        ].map(t => (
          <div class="type-row">
            <div class="type-icon">{t.icon}</div>
            <div>
              <div class="type-label">{t.label}</div>
              <div class="type-desc">{t.desc}</div>
            </div>
          </div>
        ))}
      </div>

      <!-- 안내 문구 -->
      <p class="contact-note">
        문의 시 소속·목적·필요 자료 범위를 간략히 적어주시면
        더 빠르게 안내드릴 수 있습니다.
      </p>

    </div>
  </main>
  <Footer />
</BaseLayout>

<style>
.page-header {
  background: var(--cream);
  border-bottom: 1px solid var(--bd);
  padding: 56px 0 44px;
  position: relative;
}
.page-header::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 3px;
  background: linear-gradient(90deg, var(--accent), var(--gold) 55%, transparent);
}
.page-title {
  font-family: var(--serif);
  font-weight: 600;
  font-size: clamp(24px, 3.5vw, 32px);
  color: var(--ink);
  margin-bottom: 14px;
  line-height: 1.45;
}
.page-lead {
  font-size: 15px;
  font-weight: 300;
  line-height: 1.8;
  color: var(--muted);
}

/* Email block */
.email-block {
  padding: 36px 0 40px;
  border-bottom: 1px solid var(--bd);
  margin-bottom: 40px;
}
.email-label {
  font-family: var(--roman);
  font-size: 10px;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--subtle);
  margin-bottom: 12px;
}
.email-address {
  font-family: var(--roman);
  font-size: clamp(20px, 3vw, 28px);
  font-weight: 500;
  color: var(--accent);
  text-decoration: none;
  letter-spacing: 0.02em;
  transition: color 0.15s;
  display: inline-block;
  border-bottom: 1px solid transparent;
}
.email-address:hover {
  color: var(--accent-mid);
  border-bottom-color: var(--accent-mid);
}

/* Type list */
.type-list {
  display: flex;
  flex-direction: column;
  gap: 0;
  border: 1px solid var(--bd);
  border-radius: var(--r);
  overflow: hidden;
  margin-bottom: 32px;
}
.type-row {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  padding: 16px 20px;
  border-bottom: 1px solid var(--bd);
}
.type-row:last-child { border-bottom: none; }
.type-icon {
  font-family: var(--serif);
  font-size: 18px;
  color: var(--accent);
  opacity: 0.6;
  min-width: 24px;
  padding-top: 1px;
}
.type-label {
  font-family: var(--serif);
  font-weight: 600;
  font-size: 14px;
  color: var(--ink);
  margin-bottom: 3px;
}
.type-desc {
  font-size: 13px;
  font-weight: 300;
  color: var(--muted);
  line-height: 1.55;
}
.contact-note {
  font-size: 13px;
  font-weight: 300;
  color: var(--subtle);
  line-height: 1.7;
}
</style>
